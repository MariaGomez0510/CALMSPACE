<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App de Apoyo Emocional</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>
        <h1>Bienvenido a la App de Apoyo Emocional</h1>
    </header>
    <main>
        <section id="pulsioximetro">
            <h2>Registro de Datos del Pulsioxímetro</h2>
            <label for="oxigeno">Nivel de Oxígeno (%):</label>
            <input type="number" id="oxigeno" min="0" max="100">
            <label for="frecuencia">Frecuencia Cardíaca (BPM):</label>
            <input type="number" id="frecuencia" min="0">
            <button onclick="guardarDatos()">Guardar Datos</button>
            <ul id="lista-datos"></ul>
        </section>
        <section id="respiracion">
            <h2>Técnicas de Respiración</h2>
            <p>Prueba esta técnica: Inhala por 4 segundos, sostén 4 segundos, exhala 4 segundos.</p>
        </section>
        <section id="foro">
            <h2>Foro de Apoyo</h2>
            <textarea id="mensaje" placeholder="Escribe tu mensaje"></textarea>
            <button onclick="publicarMensaje()">Publicar</button>
            <ul id="lista-mensajes"></ul>
        </section>
    </main>
    <script>
        function guardarDatos() {
            let oxigeno = document.getElementById("oxigeno").value;
            let frecuencia = document.getElementById("frecuencia").value;
            if (oxigeno && frecuencia) {
                let lista = document.getElementById("lista-datos");
                let item = document.createElement("li");
                item.textContent = `Oxígeno: ${oxigeno}%, Frecuencia: ${frecuencia} BPM`;
                lista.appendChild(item);
            }
        }

        function publicarMensaje() {
            let mensaje = document.getElementById("mensaje").value;
            if (mensaje.trim() !== "") {
                let lista = document.getElementById("lista-mensajes");
                let item = document.createElement("li");
                item.textContent = mensaje;
                lista.appendChild(item);
                document.getElementById("mensaje").value = "";
            }
        }
    </script>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f0f8ff;
            color: #333;
        }
        header {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
        }
        section {
            margin: 20px;
            padding: 10px;
            background: white;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        button {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</body>
</html>
